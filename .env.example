# Database - COPY to .env.local and set real values, never commit .env.local
DB_HOST=localhost
DB_PORT=5432
DB_USER=shakil
DB_PASS=CHANGEME_STRONG_DB_PASSWORD_min16chars
DB_NAME=peoplehub
DB_SSLMODE=disable

# API Server - generate with: openssl rand -hex 32
API_PORT=5000
JWT_SECRET=CHANGEME_min32chars_replace_with_openssl_rand_hex_32

# Frontend
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_BASE_PATH=
